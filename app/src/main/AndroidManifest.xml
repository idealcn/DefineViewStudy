<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.idealcn.define.view">

    <supports-screens android:normalScreens="true"
        />


    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ClockActivity" />
        <activity android:name=".ui.ProgressBarActivity" />
        <activity android:name=".ui.RoundCakeActivity" />
        <activity android:name=".ui.DragActivity" />
        <activity android:name=".ui.TableActivity" />
        <activity android:name=".ui.FlowLayoutActivity" />
        <activity android:name=".ui.PaddingActivity" />
        <activity android:name=".ui.TimeAxisActivity" />
        <activity android:name=".ui.PositionActivity" />
        <activity android:name=".ui.DefineViewGroupActivity" />
    </application>


</manifest>